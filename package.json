{
  "name": "riptide-backend",
  "version": "0.0.1",
  "description": "The backend used to host DiscordBot and Server.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/index.js",
    "setup-dev": "tsc --build && npm i"
  },
  "author": "cutymeo / shiinazzz",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  }
}
